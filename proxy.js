addEventListener('fetch',e=>e.respondWith(handle(e.request)));async function handle(r){const u=new URL(r.url),t=u.searchParams.get('url');if(!t)return new Response('no url',{status:400});const res=await fetch(decodeURIComponent(t),{headers:{apikey:r.headers.get('apikey')||'',Accept:'application/json'}});return new Response(await res.text(),{status:res.status,headers:{'Content-Type':'application/json','Access-Control-Allow-Origin':'*'}});}
